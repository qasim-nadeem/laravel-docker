# Laravel core
/vendor/
/node_modules/
/public/storage

# Laravel generated files
/storage/*.key
.env
.env.backup
.env.*.local
.phpunit.result.cache
.phpunit.cache

# IDEs & Editors
/.idea/
/.vscode/
/*.sublime-workspace
/*.sublime-project

# OS artifacts
.DS_Store
Thumbs.db

# Laravel stuff you might not want
Homestead.yaml
Homestead.json
/.vagrant

# SQLite (if you ever use it locally)
/*.sqlite
/*.sqlite3

# Composer
composer.phar

# NPM/Yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker overrides (optional—only ignore if you have custom override files)
/docker-compose.override.yml
/docker/.env

# Build artifacts
/public/hot
/public/mix-manifest.json
/public/css/*.map
/public/js/*.map

# Coverage reports
/coverage/
/phpunit-coverage/

# Optional: ignore Docker volume data directories if you bind-mount them
# (only if you accidentally checked them in)
/docker/mysql-data/
/docker/redis-data/

# Misc
.env.testing
